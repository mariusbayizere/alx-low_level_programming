#include<stdio.h>
#include<stdlib.h>

/**
 * add - function that will add two number
 * @x: will receive x as integer
 * @y: will receive y as integer
 * Return: return add
 */
int add(int x, int y)
{
	return (x + y);
}
