#include<stdio.h>
#include<stdlib.h>

/**
 * mod - function that will mod two number
 * @x: will receive x as integer
 * @y: will receive y as integer
 * Return: return mod
 */
int mod(int x, int y)
{
	return (x % y);
}
