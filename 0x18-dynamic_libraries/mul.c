#include<stdio.h>
#include<stdlib.h>

/**
 * mul - function that will mul two number
 * @x: will receive x as integer
 * @y: will receive y as integer
 * Return: return mul
 */
int mul(int x, int y)
{
	return (x * y);
}
