#include<stdio.h>
#include<stdlib.h>

/**
 * sub - function that will substartct two number
 * @x: will receive x as integer
 * @y: will receive y as integer
 * Return: return sub
 */
int sub(int x, int y)
{
	return (x - y);
}
